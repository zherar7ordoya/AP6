using System.Windows.Forms;

namespace MAP
{
    public static class UserControlsMapper
    {
        public static void MapClientToUserControl(Client client, UserControl userControl)
        {
            // Mapeo de los datos del cliente a los controles del formulario
        }

        public static void MapVehicleToUserControl(Vehicle vehicle, UserControl userControl)
        {
            // Mapeo de los datos del vehículo a los controles del formulario
        }
    }
}
